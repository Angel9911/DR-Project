export class City {
  // tslint:disable-next-line:variable-name
  city_id: string;
  // tslint:disable-next-line:variable-name
  city_name: string;
  offices: string[];

  // tslint:disable-next-line:variable-name
 /* constructor(city_id: string, city_name: string, offices: string[]) {
    this.city_id = city_id;
    this.city_name = city_name;
    this.offices = offices;
  } */
}
